using UnityEngine;
using System.Collections;
using UnityEditor;

// [CustomEditor(typeof(RoutineList), true)]
public class RoutineListViewer : Editor
{
    /*
    RoutineList routineList;

    void OnEnable()
    {
        // Setup the SerializedProperties.
        routineList = target as RoutineList;
    }

    public override void OnInspectorGUI()
    {
        base.OnInspectorGUI();

        // get values
        int count = routineList.GetRoutineCount();

        // show in inspector
        GUILayout.Label("Routines:", EditorStyles.boldLabel);
        GUILayout.Label("Count: " + count);
    }*/
}
